## New Features

- Added the new Gemma 4 local LLM plugin via MLX for on-device prompt processing.
- Backported the modern settings sidebar so the updated settings navigation also works on macOS 14.
- Polished the notch indicator layout and presentation for a cleaner dictation UI.

## Bug Fixes

- Fixed a plugin install path issue so installed plugin bundles are kept in the managed plugins directory instead of disappearing after install/update flows.
- Fixed WhisperKit download progress so it stays attached to the selected model.
- Fixed a hotkey event tap crash.
- Fixed missing and incorrect German localizations in multiple UI paths.
- Restored the minimal indicator sources after a merge regression.

## Other Changes

- Enabled dedicated release automation for the Gemma 4 plugin.
- Bumped the bundled Gemma 4 plugin to `1.0.1`.
